tasks:
  build:
    deps:
      - 'frontend:build'
      - 'backend:build'
  lint:
    deps:
      - 'frontend:lint'
      - 'backend:lint'
  format:
    deps:
      - 'frontend:format'
      - 'backend:format'
